#ifndef SERVERPOOL_HPP
#define SERVERPOOL_HPP

#include "total.hpp"

class ServerPool
{
    public :
        std::vector<Server> serverPool;

        ServerPool();
        ~ServerPool();
    private :

};
#endif